<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ojasvisingh.placeme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".Register" />
        <activity android:name=".Admin" />
        <activity android:name=".AdminJob" />
        <activity android:name=".CreateJob" />
        <activity android:name=".Student" />
        <activity android:name=".JobDetails" />
        <activity android:name=".AdminJobDetails" />
        <activity android:name=".EditProcess" />
        <activity android:name=".MarkAttendance" />
        <activity android:name=".EnrolledJobs" />
        <activity android:name=".Feedback" />
        <activity android:name=".QuestionBank"></activity>
    </application>

</manifest>